export default {
    // 基础url前缀
    baseURL: '/api',
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded;charset=UTF-8',
        'X-Requested-With': 'XMLHttpRequest'
    },
    timeout: 500000,
}

export const TOKEN_KEY = 'token-dq'

export const BASE_URL = '/api'
